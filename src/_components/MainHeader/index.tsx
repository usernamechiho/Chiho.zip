"use client";

import { fontSize, color } from "@/_styles";
import { usePathname } from "next/navigation";

export default function MainHeader() {
  return (
    <div>
      <p>chiho.zip</p>
    </div>
  );
}
